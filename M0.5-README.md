# DustNote · 尘心笔记

> 端到端加密 · 跨端同步 · 清新简约

## M0.5 状态

✅ **update-manifest API** — `GET /api/v1/update-manifest`  
✅ **SQLite 迁移系统** — `server/src/migrations.ts`  
✅ **客户端启动检测** — `web/src/lib/use-update-check.ts`  
✅ **强制升级遮罩** — L0/L1 阻塞 UI  
✅ **软提示 Banner** — L2/L3 非阻塞提示  
✅ **Docker 多阶段构建** — Node 20 Alpine + tini + 非 root  
✅ **冒烟测试脚本** — `scripts/smoke-test.sh`

## 目录结构

```
dustnote/
├── shared/                    # 跨端共享类型与工具
│   ├── src/
│   │   ├── version.ts         # SemVer + 强制升级判断
│   │   ├── update-check.ts    # update-manifest types + check 函数
│   │   ├── types.ts           # Note / Share / UserPreferences
│   │   └── index.ts
│   └── test/
│       └── version.test.ts
├── server/                    # Node.js 20 + Express + SQLite
│   ├── src/
│   │   ├── index.ts           # 入口
│   │   ├── app.ts             # Express
│   │   ├── env.ts             # 环境变量
│   │   ├── config-validate.ts # 启动校验
│   │   ├── db.ts              # SQLite + 迁移
│   │   ├── migrations.ts      # 迁移列表
│   │   ├── logger.ts          # pino
│   │   ├── middleware/
│   │   │   └── version-check.ts
│   │   ├── routes/
│   │   │   ├── update-manifest.ts
│   │   │   └── health.ts
│   │   └── services/
│   │       └── update-manifest.ts
│   ├── .env.example
│   └── Dockerfile
├── web/                       # Vite + React 18 + Tailwind 3
│   ├── src/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   ├── index.css
│   │   ├── lib/
│   │   │   ├── device.ts           # UUID 设备 ID
│   │   │   └── use-update-check.ts # 启动检测 hook
│   │   └── components/
│   │       ├── ForceUpdateOverlay.tsx
│   │       └── UpdateBanner.tsx
│   ├── public/favicon.svg
│   ├── index.html
│   ├── vite.config.ts
│   ├── tailwind.config.js
│   └── postcss.config.js
├── scripts/
│   └── smoke-test.sh
└── package.json                # 根 monorepo
```

## 快速开始

```bash
# 1. 安装依赖
pnpm install

# 2. 编译 shared
pnpm --filter @dustnote/shared build

# 3. 启动后端
pnpm dev:server
# → http://localhost:3210

# 4. 启动 Web（另一个终端）
pnpm dev:web
# → http://localhost:5173

# 5. 冒烟测试
bash scripts/smoke-test.sh
```

## API 测试

```bash
# 健康检查
curl http://localhost:3210/api/v1/health

# update-manifest
curl -H "X-Client-Version: 0.1.0" \
     -H "X-Client-Platform: web" \
     -H "X-Client-Channel: stable" \
     -H "X-Client-Device-Id: 11111111-1111-4111-8111-111111111111" \
     http://localhost:3210/api/v1/update-manifest
```

## Docker 构建

```bash
# 在 monorepo 根目录
docker build -f server/Dockerfile -t dustnote:0.1.0 .

# 运行
docker run --rm -p 3210:3210 dustnote:0.1.0
```
