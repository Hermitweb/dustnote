# DustNote 生产环境变量
# 复制为 .env 后修改

# 服务端口（容器内 80，宿主机映射见 docker-compose.yml）
PORT=8080

# 前端域名（CORS 白名单）
WEB_ORIGIN=https://dustnote.your-domain.com

# 服务版本
SERVER_VERSION=0.1.0

# 日志级别: debug | info | warn | error
LOG_LEVEL=info
